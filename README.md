## Integration test with use of Docker compose

Integration test for simple service written in `node.js` connecting with `Redis`.
Test written with use of `tape`.

Run integration test:
```
cd integration-test
npm test
```